# Session 1 brief — Named-operator card + Workflow strip

You are working on branch `shruti/tier1-parallel-build` in parallel with
4 other Claude Code sessions. Read `PARALLEL_SESSIONS.md` BEFORE you
start. Follow its rules. Do not touch files outside your owned list.

## Items to ship

### Item 1.1 — Named-operator card in substrate panel

A small card at the TOP of the substrate panel (above the alert list)
that carries the canonical operator-persona sentence from
`docs/liminal-custody-onepager.md`:

> A maritime watch officer at U.S. 5th Fleet, 0200 local, monitoring
> AIS disruptions near a strategic chokepoint in the Strait of Hormuz,
> deciding whether a dark-gap reappearance under a new identity deserves
> immediate escalation or second-source collection.

This is the SINGLE most pitch-relevant element for procurement
screenshots. Every screenshot must carry this sentence.

**Visual register:** small card, dark-mode operator-grade. Header line
in mono uppercase tracked-out ("NAMED OPERATOR · 5TH FLEET J2"). Body
in serif (Fraunces) at ~12px, italic, line-height 1.55. Soft left
border in `--color-decision` at 30% alpha to signal "this is who's
reading."

**File:** create `app/src/components/NamedOperatorCard.tsx` and import
into `app/src/components/SubstratePanel.tsx`. Mount above
`<CustodyQueue>`.

### Item 1.2 — Workflow strip in topbar

A small horizontal strip at the top of the topbar (or just below the
brand line) showing the four-pane workflow:

```
01 SUBSTRATE  ──▸  02 STAGE  ──▸  03 WORKING  ──▸  04 COMMAND
```

The active pane (currently focused by mouse position OR the pane the
selected case is being read in — for now, hardcode "03 WORKING" as
the active state during demo) renders in `--color-decision`; others
render in `--color-ink-tertiary`. Numerals in italic Fraunces;
labels in mono uppercase.

This complements the empty-stencil four-LAYER framework taught in
the working panel. Together they teach BOTH the pane workflow AND
the layer architecture.

**File:** create `app/src/components/WorkflowStrip.tsx` and mount
in `app/src/components/AppShell.tsx` topbar (between the brand and
the breadcrumb, OR below the topbar as a second strip).

## Owned files (EXCLUSIVE — no other session touches these)

- `app/src/components/NamedOperatorCard.tsx` (NEW)
- `app/src/components/WorkflowStrip.tsx` (NEW)
- `app/src/components/SubstratePanel.tsx` (mount the card here)
- `app/src/components/AppShell.tsx` — TOPBAR REGION ONLY (lines ~40-60).
  Do NOT touch lines below 60. S4 owns lines 80-95.

## CSS region

Append your CSS at the bottom of `app/src/styles.css` under this marker:

```css
/* ─── S1 NAMED OPERATOR + WORKFLOW STRIP ─── */
```

Do NOT edit any existing CSS rules. Only append.

## Success criteria

- `cd app && bunx tsc --noEmit` passes
- Substrate panel shows named-operator card at top, above alerts list
- Topbar shows workflow strip with `01 SUBSTRATE → 02 STAGE → 03 WORKING → 04 COMMAND`
- Workflow strip has the third item ("03 WORKING") highlighted in decision-blue
- No regressions: existing topbar elements (brand, breadcrumb, status) all still render

## Workflow

1. `git pull --ff-only`
2. Update your row in `PARALLEL_SESSIONS.md` to claimed
3. Commit and push the status update FIRST
4. Build the items
5. Type-check
6. `git pull --ff-only` again
7. Commit YOUR files only with `[S1]` prefix
8. Update status to shipped
9. Push

If anything blocks (file owned by another session, type-check fails
because of another session's commit, etc.), STOP and surface to user.
